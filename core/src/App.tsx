import React from 'react';

import { Button } from 'antd';
import AuthApp from 'auth/AuthApp';

export const App = () => (
  <div>
    <h1>Host-Remote</h1>
    <Button>Hello</Button>
    <AuthApp />
  </div>
);
